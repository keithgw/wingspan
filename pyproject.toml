[project]
name = "wingspan"
version = "0.1.0"
description = "Reinforcement learning agents for the board game Wingspan"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.3",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
